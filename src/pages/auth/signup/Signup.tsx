import { AuthWrapper, Typography } from "@/components";

type Props = {
 
}
export const Signup = ({}: Props) => {
 return ( 
  <AuthWrapper>
   <Typography as="h1">
    Signup
   </Typography>
  </AuthWrapper>
 );
}